import React, { Component } from 'react';
import { inject, observer } from 'mobx-react';
import { Container } from './CustomizeStyles';


export default class CustomizeComp extends Component {

    render() {
        return (
            <Container>
                
            </Container>
        );
    }
};
