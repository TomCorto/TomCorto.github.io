import React, { Component } from 'react';
import { inject, observer } from 'mobx-react';
import { Container } from './BikeSelectorCompStyle';

export default class BikeSelectorComp extends Component {

  render() {
    return (
      <Container>
        Selector
      </Container>
    )
  }
}
